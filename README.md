# GP

## compile sv files : cs -full64 -sverilog sampler.sv tb_hqc_sampler_top.sv     -o simv -timescale=1ns/1ps
## run : ./simv

------
## compile c file : gcc compare_ref.c -o compare_ref
## run : ./compare_ref
